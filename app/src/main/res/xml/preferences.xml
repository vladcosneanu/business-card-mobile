<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="@string/nearby_summary"
        android:entries="@array/settings_nearby"
        android:entryValues="@array/settings_nearby_values"
        android:key="nearby"
        android:summary="%s"
        android:title="@string/nearby_title"></ListPreference>
    <Preference
        android:key="logout"
        android:title="@string/action_logout" />

</PreferenceScreen>